# Lifecycle

The repository does not define agent, task, message, session, or execution states,
allowed transitions, terminal states, retries, or cancellation behavior. No lifecycle
rules should be inferred from the README's example flows.

Document lifecycle behavior here when a design is agreed and link corresponding
implementation and conformance coverage.
